export default function Button(props) {
  return (
    <button
      type="button"
      id="open-new-link-form"
      className="
        pointer-events-none
        border 
        font-medium
        px-3.5
        py-1.5
        -ml-1
        rounded-full
        border-gray-lighter
        bg-gray-faint
        hover:bg-gray-lightest
        text-black
        
    "
    >
      Add
    </button>
  );
}
